{
  "name": "amestat-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Amestat stats collector: drives the installed browser and writes to Supabase",
  "scripts": {
    "run": "node run.mjs",
    "watch": "node watch.mjs",
    "proxy-check": "node proxy-check.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.115.0",
    "playwright-core": "1.63.0"
  }
}
